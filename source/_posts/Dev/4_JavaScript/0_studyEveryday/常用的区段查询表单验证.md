---
title: '常用的区段查询表单验证'
date: 2023-03-06 15:47:44
copyright_info: The copyright of this article is owned by Zhang Yuhan, and it follows the CC BY-NC-SA 4.0 agreement. For reprinting, please attach the original source link and this statement
categories: 
  - 'Dev'
  - 'JavaScript'
  - 'studyEveryday'
tags: 
  - 'Dev'
  - 'JavaScript'
  - 'studyEveryday'
  - '常用的区段查询表单验证'
---
## 1.正整数区段查询
支持全不填，全填，和单独填任意一个

html代码
```html
<div class="layui-input-inline">  
    <input type="text" id="sjtjStartMile" placeholder="开始里程（m）"  
           autocomplete="off"  
           class="layui-input">  
</div>  
<div class="layui-form-mid">-</div>  
<div class="layui-input-inline">  
    <input type="text" id="sjtjEndMile" placeholder="结束里程（m）"  
           autocomplete="off"  
           class="layui-input">  
</div>
```

js代码
```js
// 表单验证开始
let startMile = $("#sjtjStartMile").val()  
let endMile = $("#sjtjEndMile").val()  
if (isNaN(startMile) || Number(startMile) < 0) {  
    Hussar.info("起始里程必须大于等于0")  
    return  
}  
if (isNaN(endMile) || Number(endMile) < 0) {  
    Hussar.info("终止里程必须大于等于0")  
    return  
}  
if (startMile && endMile && Number(startMile) >= Number(endMile)) {  
    Hussar.info("起始里程必须小于终止里程")  
    return  
}
// 表单验证结束
```
